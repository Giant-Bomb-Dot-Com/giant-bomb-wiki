{{> header }}

<div class="platforms-page">
    <div class="platforms-header">
        <h1>Platforms</h1>
        <div
            data-vue-component="ViewToggle"
            data-target-container=".platforms-page"
            data-storage-key="platformViewPreference">
        </div>
    </div>

    <div class="platforms-content">
        <aside class="platforms-sidebar">
            <div
                data-vue-component="PlatformFilter"
                data-current-letter="{{{currentLetter}}}"
                data-current-sort="{{{currentSort}}}">
            </div>
        </aside>

        <div class="platforms-main-wrapper"
            data-vue-component="PlatformList"
            data-initial-data="{{{vue.platformsJson}}}"
            data-total-count="{{{totalCount}}}"
            data-current-page="{{{currentPage}}}"
            data-total-pages="{{{totalPages}}}">
        </div>
    </div>
</div>

